{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "cli": "nx",
  "title": "DeployCommands executor",
  "description": "",
  "type": "object",
  "properties": {
    "commands-path": {
      "type": "string",
      "description": ".ts filepath to load commands"
    }
  },
  "required": ["commands-path"]
}
